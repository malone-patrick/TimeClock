import { AvailablePipePipe } from './available-pipe.pipe';

describe('AvailablePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AvailablePipePipe();
    expect(pipe).toBeTruthy();
  });
});
